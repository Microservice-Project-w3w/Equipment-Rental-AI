[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "equipment-rental-ai"
version = "0.1.0"
description = "AI Chatbot Service for Equipment Rental"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic-settings",
    "python-dotenv",
    "pyjwt",
]
